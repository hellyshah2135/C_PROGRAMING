// 7. Area of Rectangle

#include <stdio.h>

int main() {
    int w, l;
    printf("Enter width and length: ");
    scanf("%d %d", &w, &l);

    printf("Area: %d\n", w * l);
    return 0;
}

